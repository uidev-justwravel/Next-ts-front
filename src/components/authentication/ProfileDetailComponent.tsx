import { Button } from "@mui/material";
import React from "react";

const ProfileDetailComponent = () => {
  return (
    <Button variant="outlined" sx={{ marginTop: "4px", fontSize: "12px" }}>
      Profile
    </Button>
  );
};

export default ProfileDetailComponent;
